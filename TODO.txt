**Documentation**
    -Finish documenting and formatting AppMenu helper classes.
    -PokeLookAndFeel is missing some @param tags

**Appearance/Style Tweaks**
    -Replace the SettingsListPage opening button with an icon.

**Wifi Improvements**
    -Update the HomePage wifi icon to better use new wifi features.
    -Add a Listener interface to WifiAccessPoint objects
    -Make the wifi page regularly scan for updates when open.
    -Wifi error: libnm-glib-WARNING nm-access-point.c:310 ap_ssid == NULL
    -Add a "Forget saved connection" button to saved wifi access points

**Misc. Bug Fixes**
    -Set password page isn`t hiding the current password field!
    -Figure out why the program hangs on authentication
    -Program hangs if xdotool is not installed

**Settings Overhaul**
    -Figure out a way to save calibration data and write it to the right file
     automatically 
    -Define page background color/image in config, replace the background
     selector on HomePageSettings.
    -Re-add the IP label to the Settings Page, add option to display the IP 
     label on the home page
    -implement UI settings page
    -configurable keyboard shortcuts on input page

**Admin and Authentication**
    -Write a proper setuid program to handle root actions.
    -Create custom PolKit policies for approving various admin actions.
    -Consider implementing a custom PolKit authentication agent.

-Add an icon selection page that uses IconThread's index of icon files

-Make bluetooth work and properly implement the bluetooth page
    -Adapt bluetooth code from 
        https://git.kernel.org/pub/scm/bluetooth/bluez.git/tree/client/main.c

-More thorough implementation of the Desktop entry standard

-Add support for .Desktop Actions

-Add support for nested folders

-Add .xpm image support

-Fix rendering issues with some .svg icons

-Design and implement TabbedAppMenu

